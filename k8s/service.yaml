apiVersion: v1
kind: Service
metadata:
  name: fast-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: fast-api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    nodePort: 30081
